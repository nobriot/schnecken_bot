[build]
# Tokio-console subscriber requires the unstable flag when building:
#rustflags = ["--cfg", "tokio_unstable"]


[env]
RUST_LOG = "debug"

[doc]
browser = "firefox" # browser to use with `cargo doc --open`,
